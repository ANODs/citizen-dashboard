[phases.setup]
nixPkgs = ['nodejs'] # Убираем npm, так как он может быть включён в nodejs по умолчанию

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview'
